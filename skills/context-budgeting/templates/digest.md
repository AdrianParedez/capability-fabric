# Compaction digest: <run-id>: <timestamp>

> Copy, fill, save to notes/. After saving, reinitialize context and reload:
> goal + this digest + active plan step + pointers.

## Goal (verbatim)
<exact goal / success criteria, do not paraphrase>

## Plan & position
- [x] Step 1: <one line outcome>
- [x] Step 2: <one line outcome>
- [ ] Step 3 (ACTIVE): <what's in progress>
- [ ] Step 4: <pending>

## Open decisions (with why)
- <decision>, chosen <option> because <constraint>; alternatives rejected: <one line>

## Last verified result
<the most recent result that passed verifying-reasoning>

## Pointers (re-fetchable, not inlined)
- <artifact> -> <path / URL / line range>
- <artifact> -> <path / URL / line range>

## Rejected paths (1 line each, so we don't repeat them)
- tried <X>, rejected because <Y>
